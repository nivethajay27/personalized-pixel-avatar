import type { AvatarConfig } from "@pixel/shared-types";

export const DEFAULT_AVATAR_CONFIG: AvatarConfig = {
  background: "sky",
  skin: "tan",
  eyes: "classic",
  hair: "short",
  accessory: "none"
};
